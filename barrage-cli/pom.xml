<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>barrage-parent</artifactId>
        <groupId>com.barrage</groupId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>barrage-cli</artifactId>

    <dependencies>
        <dependency>
            <groupId>com.barrage</groupId>
            <artifactId>barrage-engine</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.barrage</groupId>
            <artifactId>barrage-protocol</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>native</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.graalvm.buildtools</groupId>
                        <artifactId>native-maven-plugin</artifactId>
                        <version>0.10.3</version>
                        <extensions>true</extensions>
                        <configuration>
                            <imageName>barrage</imageName>
                            <mainClass>com.barrage.cli.Main</mainClass>
                            <buildArgs>
                                <arg>-H:+UnlockExperimentalVMOptions</arg>
                                <arg>-H:+SharedArenaSupport</arg>
                                <arg>-H:+ForeignAPISupport</arg>

                                <arg>--enable-native-access=ALL-UNNAMED</arg>
                                <arg>-O4</arg>

                                <arg>--initialize-at-run-time=com.barrage.protocol.HTTP.HttpMessage,com.barrage.kernel.io.NativeSocket,com.barrage.engine.ServerEngine</arg>

                                <arg>--no-fallback</arg>
                            </buildArgs>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-shade-plugin</artifactId>
                        <version>3.5.1</version>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals><goal>shade</goal></goals>
                                <configuration>
                                    <transformers>
                                        <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                            <mainClass>com.barrage.cli.Main</mainClass>
                                        </transformer>
                                    </transformers>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>